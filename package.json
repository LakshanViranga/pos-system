{
  "name": "restaurant-pos",
  "version": "1.0.0",
  "description": "Restaurant POS System",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run vite\" \"tauri dev\"",
    "vite": "vite",
    "build": "vite build",
    "tauri": "tauri",
    "tauri-build": "npm run build && tauri build",
    "tauri-build-win": "npm run build && tauri build --target x86_64-pc-windows-msvc"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "vue": "^3.3.0",
    "vue-router": "^4.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "@vitejs/plugin-vue": "^4.2.0",
    "concurrently": "^10.0.3",
    "vite": "^4.3.0"
  }
}
